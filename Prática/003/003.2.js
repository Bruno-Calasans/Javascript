//Number() converte uma string para número
//let age = Number(prompt('Digite sua idade'))
age = 0
// Converte o número para Float
age = Number.parseFloat(age)

// Converte o número para Inteiro
age = Number.parseInt(age)

//Função String() converte uma string para Number
age = String(age)

// Método .toString() também faz isso
age = age.toString()

var n = 19.7876867867
n = n.toFixed(2)
console.log(n)

// Função Boolean() converte os dados para boolean
console.log(Boolean(false))