
    // objeto history

    // usando o lenght
    console.log(history.length)

    // usando o history.back()
    history.back()

    // usando o history.forward()
    history.forward()

    // usando o history.go(x)
    history.go(2) // avança duas páginas
    history.go(-2) // volta duas páginas